.container-fluid
  %h2 Albums

  %table.table.table-hover.table-responsive
    %thead
      %tr
        %th Title
        %th Author
        %th Images Count
        %th Actions
    %tbody
      - @albums.each do |album|
        %tr
          %td
            = album.title
          %td
            = album.user.email
          %td
            = album.photos.count
          %td
            = link_to 'Check Images', visitor_path(album), class: 'btn btn-info'
