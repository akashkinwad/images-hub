.container-fluid
  %h3
    = "Album : #{@album.title}"
  %h3
    = "Author : #{@album.user.email}"
  %br/
  - @photos.in_groups_of(3, false) do |row|
    .row
      - for photo in row
        - if photo.image.attached?
          .col-lg-4.col-md-4.col-xs-12.thumb
            %strong Tagline:
            %strong
              = photo.tagline
            %br/
            %a.thumbnail
              = image_tag photo.image.variant(resize: "400x300"), class: 'img-responsive'

  = link_to 'Back', album_visitors_path, class: 'btn btn-warning'